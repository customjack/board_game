import BaseModal from '../BaseModal.js';

/**
 * PromptBaseModal - lightweight base for prompt-style modals.
 */
export default class PromptBaseModal extends BaseModal {
    constructor(config = {}) {
        super(config);
    }
}
